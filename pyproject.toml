[project]
name = "wireless-adb-transfer"
version = "1.0.0"
description = "Easy wireless ADB file transfer between PC and Android devices"
readme = "README.md"
requires-python = ">=3.10.2"
dependencies = [
    "adbutils>=2.9.3",
]

[project.scripts]
adb-transfer = "adb_file_transfer.transfer:main"
